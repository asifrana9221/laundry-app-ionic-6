import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.washzambia.owner',
  appName: 'Owner Laundry',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
